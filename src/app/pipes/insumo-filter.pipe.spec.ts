import { InsumoFilterPipe } from './insumo-filter.pipe';

describe('InsumoFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new InsumoFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
