import { AlmacenFilterPipe } from './almacen-filter.pipe';

describe('AlmacenFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AlmacenFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
