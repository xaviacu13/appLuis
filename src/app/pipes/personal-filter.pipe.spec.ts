import { PersonalFilterPipe } from './personal-filter.pipe';

describe('PersonalFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PersonalFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
